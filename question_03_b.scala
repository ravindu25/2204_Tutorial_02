object Q4 extends App{
    var (a,b,c,d)=(2,3,4,5);
    var k = 4.3f;


    // println(--b * a + c*d--);
    // println(a++);
    println (-2 * ( g - k ) +c); 
    // println (c=c++);
    // println (c=++c*a++);

    //Postfix and prefix operators are not supported in Scala. Therefore i have commeneted those lines to avoid errors.
}